{
  "name": "poc-gcp-danilo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "scripts": {
    "install-dep": "npm install && cd functions && npm install && npm run build",
    "start": "firebase emulators:start --only functions,firestore",
    "test": "cd functions && npm run test",
    "test:cov": "cd functions && npm run test:cov"
  },
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^12.2.0",
    "firebase-functions": "^5.0.1",
    "firebase-tools": "^13.13.0"
  },
  "engines": {
    "node": "20"
  },
  "devDependencies": {
    "ts-node": "^10.2.1",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^4.5.2"
  }
}
